- content_for :splash do
  .home-page-splash
    .home-page-splash-image
      = image_tag("home-page-splash.jpg", class: "img-fluid")
    %h1.strong Do Good, <br> Get Fit
    .home-page-splash-text
      %p
        We are a running club that does good.
        %br/
        To reach out to the elderly, meet like minded people and become in community activities.
      %p
        We have completed
        %strong 24,000
        runs and good deeds to date.

.text-sm-center
  %form.form-inline
    .form-group
      %h4 Find your nearest run
      %label.sr-only{ placeholder: "Postcode" }
      .input-group
        %input.form-control{ placeholder: "Postcode" }
        %span.input-group-btn
          =link_to "Search", "/search", class: "btn btn-primary"

.text-sm-center
  %h2 3 reasons to run

.reasons
  .reason.group-runs
    = link_to "/group-runs" do
      %h3.strong Group runs
      %p.lead
        Fancy something new?
        Meet new people and make your area a better place.
      %div
        .btn.btn-secondary More info
  .reason.missions
    = link_to "/missions" do
      %h3.strong Run missions
      %p.lead
        One off mission.
        Make
        %strike important deliveries to an isolated older person.
      %div
        .btn.btn-secondary More info

  .reason.coaches
    = link_to "/run-to-an-older-person" do
      %h3.strong Run to an older person
      %p.lead
        Regular pairing with weekly runs to older person.
        They motivate your run, you motivate their life.
      %div
        .btn.btn-secondary More info

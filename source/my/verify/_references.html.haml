.card
  .card-header#headingReferences{ role: "tab" }
    = progress_tag(:required)
    %h5.mb-0
      %a.collapsed{ "aria-controls": "references", "aria-expanded": "false", "data-parent": "#accordion", "data-toggle": "collapse", href: "#references" }
        References
  .collapse#references{ "aria-labelledby": "headingReferences",  role: "tabpanel" }
    .card-block
      - if runner.references.empty?
        %p
          References help assess whether you are suitable to visit an isolated older person.

        %form.form-inline
          .row
            %fieldset.form-group.col-md-6
              %h5 Professional Reference
              %p
                Someone you have worked with in a
                %a{ href:"", data: { toggle: "tooltip", "original-title": "It can be your line manager, a colleague or your tutor/lecturer you are a student." } } professional context
                for at least a year.

              .form-group
                %label.sr-only{ for: "name" } Name
                %input.form-control{ placeholder: "Name" }
              .form-group
                %label.sr-only{ for: "email" } Email
                %input.form-control{ placeholder: "Email" }

            %fieldset.form-group.col-md-6
              %h5 Personal Reference
              %p Someone who knows you inside out, who you've known for 2 years or more.

              .form-group
                %label.sr-only{ for: "name" } Name
                %input.form-control{ placeholder: "Name" }
              .form-group
                %label.sr-only{ for: "email" } Email
                %input.form-control{ placeholder: "Email" }

          .row
            .col-md-12
              %br
              %p We will contact your references by email and ask them to provide some feedback.
            .text-xs-center
              %button.btn.btn-primary{ type: "submit" } Submit

      - elsif runner.reference_checked?
        All done.
      - else
        %ul
          - runner.references.each do |reference|
            %li
              = reference.type
              = reference.referee.name
              = reference.referee.email
              = reference.completed
              = reference.accepted

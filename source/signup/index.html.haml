.row
  .col-md-6.col-md-offset-3.centered.splash-box
    %h1.logo Goodgym
    %h2 Get Running

    %p{style: "margin-bottom: 2em; text-align: left;"} Get started with GoodGym and we’ll keep you up to date with runs in your area.  If we’re not operating in your area yet, we can connect you with others like you.  Once you’ve come on a run you can become a full member.  We won’t share your info with any third parties.

    .row
      .col-md-8.col-md-offset-2.centered
        %form
          .form-group.string
            %div
              %input{ placeholder: "Name" }
          .form-group.string
            %input{ placeholder: "Email" }
          .form-group.string
            %input{ password: "Password" }
          .form-group.string
            %input{ password: "Confirm password" }
          = link_to "Get started", "/my/verify"

        %p.or-divider or

        %p= link_to "<span class='socicon socicon-facebook' style='text-transform: lowercase;'></span> Get started with Facebook".html_safe, "/auth/facebook", :class => "btn btn--facebook btn-block", :id => "facebook-home"

        %hr

        %p Already got an account? #{ link_to "Sign in here", login_path }
